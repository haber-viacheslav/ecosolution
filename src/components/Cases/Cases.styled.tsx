import styled from 'styled-components';

export const StyledCasesTitle = styled.h2`
  color: ${(props) => props.theme.colors['dark-green']};
  font-size: 28px;
  font-style: normal;
  font-weight: 400;
  line-height: 1;
  text-transform: uppercase;
  text-align: left;
  margin: 0;
  width: 320px;
`;
